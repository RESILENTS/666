class username_check_a7:
    # конструктор
    def __init__(self, name):
        self.name = username_check_a
 
    def display_info(self):
        bot.send_message(message.chat.id, "Введите данные", self.name)
